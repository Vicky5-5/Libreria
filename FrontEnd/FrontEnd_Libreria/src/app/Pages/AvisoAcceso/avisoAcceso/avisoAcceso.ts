import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-aviso-acceso',
  standalone: true,
  imports: [],
  templateUrl: './avisoAcceso.html',
  styleUrl: './avisoAcceso.css',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AvisoAcceso { }
